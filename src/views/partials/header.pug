header
    div.header
        nav.navigationWrapper
            div.logoWrapper
                span.stylish
                    a(href='/') 레미코드
                span.logo for Mixer Truck Driver
            div.linkWtapper
                if loggedIn
                    ul.navigation
                        li.parent
                            a.link(class="link", href=`/record/write/${loggedInUser._id}`) 오늘의기록
                        li.parent
                            a(class="link", href=`/record/edit/${loggedInUser._id}`) 기록수정
                        li.parent
                            a(class="link", href=`/record/history/${loggedInUser._id}`) 기간별조회
                else
                    ul.navigation
                        li.parent
                            a(class="link", href="/user/join") 회원가입
        if loggedIn
            div.profileWrap
                div
                    small
                        span
                            a(class="link", href=`/user/edit/${loggedInUser._id}`) #{loggedInUser.name}
                            |님의 프로필
                        span
                            a(class="link", href="/user/logout") 로그아웃